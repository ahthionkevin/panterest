{% extends 'base.html.twig' %}

{% block title %} {{ pin.getTitle() }}{% endblock %}

{% block body %}

    <a href="{{path('app_home',{id:pin.id})}}">Go back</a>
    <a href="{{path('app_pin_edit')}}">Edit</a>
    <h1>{{ pin.getTitle() }}</h1>
    <p>{{ pin.getDescription() }}</p>


{% endblock %}