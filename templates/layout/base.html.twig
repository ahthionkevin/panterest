<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}

        <style>
            .alert
            {
                padding: 10px;
                text-align: center;
            }

            .alert-success
            {
                background-color: green;
                color: white;
            }

            .alert-error
            {
                background-color: red;
                color: white;
            }

            .alert-info
            {
                background-color: blue;
                color: white;
            }
        </style>
    </head>
    <body>
        {{ include("layout/parshal/_flashInfo.html.twig")}}

        <nav>
            <ul>
                <li><a href="{{ path('app_home') }}">Acceuil</a></li>
                <li><a href="{{ path('app_create_pin') }}">Create Pin</a></li>
            </ul>
        </nav>

        {% block body %}{% endblock %}
    </body>
</html>
